VITE_DATA_MODE=demo
# Leave blank for same-origin `/api` calls. Vite proxies them to FastAPI in dev;
# set this to a separately hosted API origin only when no reverse proxy exists.
VITE_API_URL=
VITE_SUPABASE_URL=https://zxjaccusnmunjgktzkme.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=
